export function c(num: number) {
  if (num > 0) {
    throw new Error();
  }
  return num;
}

export function d(num: number) {
  if (num > 2) {
    throw new Error();
  }
  return num;
}

export function e(num: number) {
  if (num > 1) {
    throw new Error();
  }
  return num;
}
